services:
  # Run Rest-API in dev configuration
  hose-api:
    build:
      dockerfile: Dockerfile.dev
    ports:
      - "8080:8080"
    volumes:
      - ./server/hose:/app
      - ~/.gradle:/home/gradle/.gradle

  # Expose DB port
  postgres:
    ports:
      - "5432:5432"
